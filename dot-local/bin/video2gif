#!/usr/bin/env nix-shell
#! nix-shell -i bash --pure
#! nix-shell -p bash ffmpeg_7-headless



set -euo pipefail

src=$1
shift

name=$(basename -- "$src")
name="${name%.*}"
dir=$(dirname "$src")

set -x
ffmpeg -i "$src" $@ "${dir}/${name}.gif"
